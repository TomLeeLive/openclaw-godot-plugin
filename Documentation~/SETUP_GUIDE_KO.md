# OpenClaw Godot Plugin - 셋업 가이드

다양한 사용 사례에 맞게 OpenClaw Godot Plugin을 설정하는 방법을 설명합니다.

## 🅰️ 모드 A: OpenClaw Gateway (원격 접속)

**언제 사용?** Telegram, Discord, 웹에서 원격으로 게임 개발하고 싶을 때

### 설정 단계

```bash
# 1. OpenClaw 설치
npm install -g openclaw

# 2. Gateway 시작
openclaw gateway start

# 3. Godot 플러그인 설치
#    addons/openclaw 폴더를 프로젝트의 addons 폴더에 복사

# 4. 플러그인 활성화
#    Project > Project Settings > Plugins > OpenClaw > Enable

# 5. 채팅 연동 설정 (선택)
openclaw config
```

### 작동 방식

```
┌──────────────┐      ┌─────────────────┐      ┌──────────────┐
│  Telegram/   │ ───→ │    OpenClaw     │ ───→ │    Godot     │
│  Discord/Web │ ←─── │    Gateway      │ ←─── │    Editor    │
└──────────────┘      └─────────────────┘      └──────────────┘
      폰                  내 컴퓨터               내 컴퓨터
```

### 사용 예시

폰에서:
```
나: "현재 씬에 있는 노드 알려줘"
AI: 8개 노드 발견: Node2D, Sprite2D, Player, Enemy, ...

나: "Player를 (100, 200)으로 이동해"
AI: 완료. Player를 (100, 200)으로 이동했습니다

나: "게임 실행해"
AI: 게임 시작됨
```

---

## 🅱️ 모드 B: MCP 직접 연결 (로컬 개발)

**언제 사용?** Claude Code나 Cursor에서 Godot를 직접 제어하고 싶을 때

### 설정 단계

```bash
# 1. Godot 플러그인 설치 (위와 동일)

# 2. MCP 서버 의존성 설치
cd /path/to/openclaw-godot-plugin/MCP~
npm install

# 3. Claude Code에 MCP 서버 등록
claude mcp add godot -- node /full/path/to/openclaw-godot-plugin/MCP~/index.js

# 4. Godot에서 MCP Bridge 활성화
#    Project > Tools > OpenClaw > Start MCP Bridge

# 5. Claude Code 사용
claude
```

### 작동 방식

```
┌──────────────┐      ┌─────────────────┐      ┌──────────────┐
│ Claude Code  │ ───→ │   MCP Server    │ ───→ │    Godot     │
│ 또는 Desktop │ ←─── │   (Node.js)     │ ←─── │    Editor    │
└──────────────┘      └─────────────────┘      └──────────────┘
     터미널            localhost:27183         localhost:27183
```

### 사용 예시

```
$ claude
> 프로젝트의 모든 씬 목록 알려줘
AI: 5개 씬 발견: main.tscn, player.tscn, enemy.tscn, ...

> "Background"라는 이름의 Sprite2D 노드 생성해
AI: Sprite2D 노드 "Background" 생성 완료

> 게임 실행하고 Space 키 눌러
AI: 게임 실행 중, Space 키 시뮬레이션
```

---

## 🔀 하이브리드 모드 (둘 다 사용)

두 모드를 동시에 사용할 수 있습니다.

| 서비스 | 기본 포트 |
|--------|----------|
| MCP Bridge | 27183 |
| OpenClaw Gateway | 18789 |

---

## 빠른 참조

| 작업 | 명령어 |
|------|--------|
| Gateway 시작 | `openclaw gateway start` |
| MCP 추가 | `claude mcp add godot -- node /path/to/MCP~/index.js` |
| MCP Bridge 시작 | Project > Tools > OpenClaw > Start MCP Bridge |
